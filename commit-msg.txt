Fix 8 remaining partial items from 31-requirement audit

1. Date pickers: Added min attributes to edit-event-form phase inputs,
   cancel-postpone-buttons, admin-event-edit-form, and max on birthdate
2. Map: Google Maps link is now optional in event-form (label changed
   from "Google Maps link *" to "Google Maps link (preferred)")
3. Organizer name: Removed truncate, using break-words so long names
   wrap instead of being clipped
4. Quantity safety: Server-side now enforces quantity >= quantitySold
   in updateEvent, not just client-side min attribute
5. Sorting: Waitlist and revenue sort now use real analytics data
   (waitlistCount and grossRevenuePaise) passed via analyticsMap
6. Waitlist atomicity: autoOfferWaitlist now uses the atomic
   offer_waitlist_next RPC (SELECT FOR UPDATE) instead of non-atomic
   JS select-then-update. RPC updated to return the offered row so
   the app can create a notification. expireWaitlistOffers is now
   called when organizer opens the event dashboard.
7. Cancelled ticket: Added "Contact organizer for refund details"
   mailto link on cancelled ticket cards. Ticket type extended with
   organizerContactEmail, fetched in listMyTickets.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
