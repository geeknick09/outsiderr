# Agent Guidelines

The working rules live in **`docs/rules.md`** — read that first, every session.

Read order: `docs/rules.md` → `docs/task.md` (pick task) → `docs/memory.md` (prior fixes) → `docs/architecture.md` (find module) → `PRODUCT_VISION.md` (product calls).

This file is kept as a pointer because tools auto-discover `AGENTS.md`.
